- name: conditions
  hosts: local
  connection: local
  vars:
    number: 12
  tasks:
  - name: check number is less than 10 or not
    ansible.builtin.debug:
      msg: "Give number {{ number }} is less than 10
    whwn: number < 10